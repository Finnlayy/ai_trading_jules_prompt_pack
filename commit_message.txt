feat: Implement Swarm War Room & Unified Broker Command Center

- Goal 1: Added backend endpoints `/agentic/swarm-state/{run_id}` and `/agentic/runs/latest`. Implemented helper `_generate_swarm_commentary` to produce esports narrative. Updated UI `frontend.html` with Swarm War Room SVG Graph visualization and Commentary Ticker.
- Goal 2: Added `UnifiedBrokerState` schema and endpoint `/broker/unified-state` to standardize account and position data. Implemented UI `Command Center` replacing existing disparate broker panels. Added kill switch, strategy rotations, and manual veto functionality.
- Added corresponding unit and mocked e2e placeholder tests. Fixed test suite conflicts.
