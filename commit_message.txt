feat(backtest): add dynamic base64 equity/drawdown charts

- Generate headless Matplotlib/Seaborn chart in backtest endpoint.
- Convert PnL to Equity & Drawdown curves in Dark Terminal Theme.
- Wrap generation in asyncio.to_thread to prevent blocking event loop.
- Refactor frontend to render base64 chart instead of absolute positioned dots.
