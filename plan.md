1. **Explore Codebase**: Scan the backend files (primarily in `app/api/` and `app/services/` and `app/main.py`) to discover implemented features.
2. **Synthesize Features**: Categorize the discovered modules into a comprehensive list of features (e.g., Trading, Risk Management, AI, Market Analysis).
3. **Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.**
4. **Present the compiled list of features to the user.**
