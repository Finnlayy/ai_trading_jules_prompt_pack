1. **Submit the PR**
   - The optimizations to `app/services/statistical_battery.py` and `app/services/cisd_scorer.py` / `app/scripts/hypeusdt_1m_backtest_v23.py` are complete.
   - The master branch merge conflict and behavioral test breakage have been solved and tested.
   - A journal entry has been added to `.jules/bolt.md`.
   - The `submit` command will be run next with a PR title '⚡ Bolt: Vectorize Hurst calculation and remove any() generators in hot paths'.
