🧹 [Code Health] Refactor `_open_position` in `pionex_direct_broker.py`

🎯 **What:**
Extracted complex validation and balance-fetching logic from the overly long `_open_position` method into two separate helper methods (`_validate_open_position` and `_get_balance_for_sizing`) within `app/services/pionex_direct_broker.py`.

💡 **Why:**
The `_open_position` function had grown too long, mixing business logic (like trade sizing and result formatting) with validation checks and API data retrieval. Extracting these steps into dedicated helper methods significantly improves code readability and maintainability without altering existing functionality.

✅ **Verification:**
1. Confirmed syntax correctness using `python -m py_compile app/services/pionex_direct_broker.py`.
2. Verified unit tests passed using `python -m pytest tests/services/test_pionex_direct_broker.py`.
3. Ran the complete QA suite (`run_qa.sh`) ensuring no regressions occurred anywhere in the backend or E2E tests.

✨ **Result:**
A much cleaner and easier-to-read `_open_position` method, making future modifications and bug finding more straightforward.
