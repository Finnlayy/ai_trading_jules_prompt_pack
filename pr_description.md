🎯 What
Scan the backend for its features and write a list with all features.
Also performed a gap analysis checking what is represented in the user interface and suggested structured integration of the missing features.

💡 Why
Requested by the user to understand the feature set and find out what needs to be integrated into the UI.

✅ Verification
Compiled the lists in `backend_features_list.md`, `ui_features.md`, and `gap_analysis.md`. The final `backend_features_list.md` contains the backend features and the structured UI suggestions.

✨ Result
A comprehensive summary of the backend features and a gap analysis of the UI representations are now available for the user.
